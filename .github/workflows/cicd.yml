name: Docker CI/CD

on:
  push:
    branches: [main]
  pull_request:

jobs:
  build-and-push:
    if: github.event_name == 'push'
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to Docker Registry
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.REGISTRY_USERNAME }}
          password: ${{ secrets.REGISTRY_TOKEN }}

      - name: Build and push image
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          tags: |
            ${{ secrets.REGISTRY_USERNAME }}/${{ secrets.IMAGE_NAME }}:latest
            ${{ secrets.REGISTRY_USERNAME }}/${{ secrets.IMAGE_NAME }}:sha-${{ github.sha }}

  container-test:
    runs-on: ubuntu-latest
    needs: build-and-push
    steps:
      - name: Pull image
        run: docker pull ${{ secrets.REGISTRY_USERNAME }}/${{ secrets.IMAGE_NAME }}:latest

      - name: Run container
        run: docker run -d -p 5000:5000 --name test ${{ secrets.REGISTRY_USERNAME }}/${{ secrets.IMAGE_NAME }}:latest

      - name: Wait for server
        run: sleep 5

      - name: Test endpoints
        run: |
          curl -f http://localhost:5000/
          curl -f http://localhost:5000/healthz | grep "ok"

      - name: Clean up
        run: docker rm -f test
